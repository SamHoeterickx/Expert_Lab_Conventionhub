import type { FC } from "react";
import { Header } from "../../../shared/components";

export const Contribute: FC = () => {
    return(
        <Header
            title={ "CONTRIBUTE" }
        />
    )
}