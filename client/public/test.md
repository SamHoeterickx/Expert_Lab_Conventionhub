
# JavaScript Code Conventions

## 🧱 Core Principles- Write
 **clear, consistent, and logical** code.- Avoid unnecessary complexity.- Code should be readable by others — not just by you.## ✍️ Naming Conventions### Variables and Functions- Use **camelCase** for variable and function names.  \`\`\`js  let userAge = 25;  function getUserName() { ... }  \`\`\`- Names should be descriptive — avoid abbreviations.  \`\`\`js  // ❌ Bad  let usrNm;  // ✅ Good  let userName;  \`\`\`### Classes- Use **PascalCase** for classes and constructor functions.  \`\`\`js  class UserProfile { ... }  \`\`\`### Constants- Use **SCREAMING_SNAKE_CASE** for constants.  \`\`\`js  const MAX_USERS = 10;  \`\`\`## 📄 Structure & Formatting### Indentation- Use **2 spaces** per indentation level (no tabs).### Semicolons- Always **use semicolons explicitly**.  \`\`\`js  const user = "Alex";  console.log(user);  \`\`\`### Quotes- Use **single quotes** for strings.  \`\`\`js  const greeting = \'Hello world\';  \`\`\`### Spacing and Blank Lines- Leave **blank lines** between logical code blocks.- Add **spaces** around operators and after commas.  \`\`\`js  const total = price + tax;  \`\`\`## 🧠 Best Practices### Use \`const\` and \`let\`- Use \`const\` by default.- Use \`let\` only when reassignment is needed.  \`\`\`js  const PI = 3.14;  let counter = 0;  \`\`\`### Use Strict Equality- Always use \`===\` and \`!==\` instead of \`==\` or \`!=\`.  \`\`\`js  if (value === 0) { ... }  \`\`\`### Avoid Global Variables- Use modules or functions to limit scope.### Use Clear Function Names- Functions should start with a **verb**.  \`\`\`js  function calculateTotal() { ... }  function fetchUserData() { ... }  \`\`\`## 📦 Imports and Exports- Use ES6 modules.  \`\`\`js  import { getUser } from \'./user.js\';  export function createUser() { ... }  \`\`\`## 🔍 Commenting Style### Single-line Comments\`\`\`js// Calculate total price including VAT\`\`\`### Multi-line Comments\`\`\`js/** * Processes a user\'s order. * @param {Object} order - The order data. * @returns {boolean} True if successfully processed. */\`\`\`## 🚫 Avoid- \`var\` — use \`let\` or \`const\`- Magic numbers or strings:  \`\`\`js  // ❌ Bad  if (status === 3) { ... }  // ✅ Good  const STATUS_ACTIVE = 3;  if (status === STATUS_ACTIVE) { ... }  \`\`\`## ✅ Example\`\`\`js// ✅ Correct styleconst MAX_USERS = 10;class User {  constructor(name) {    this.name = name;  }  greet() {    console.log(\`Hello, \${this.name}!\`);  }}function createUsers(names) {  return names.map((name) => new User(name));}const users = createUsers([\'Alex\', \'Jamie\']);users[0].greet();\`\`\`**Last updated:** November 2025  **Author:** [Team or Organization Name]
